CREATE TABLE playlist (
                                 name varchar(70) NOT NULL,
                                 duration int NOT NULL,
                                 now_playing boolean NOT NULL
);